{
  "$defs": {
    "SignalIntent": {
      "description": "Strategy intent. Only BUY/SELL may later become candidate orders.",
      "enum": [
        "BUY",
        "SELL",
        "HOLD"
      ],
      "title": "SignalIntent",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Strategy trading intent (design.md §6.2).\n\n``strength`` is a signed internal score in ``[-1, 1]``. It is not a success\nprobability, profit probability, or sizing weight. ``HOLD`` signals are\ninformational only and must never become broker orders.\n\nStructural construction via ``model_validate`` does not prove the signal is\nactive. Downstream consumers must use :func:`parse_trade_signal` or\n:func:`parse_trade_signal_for_context`, which require an evaluation ``as_of``.",
  "properties": {
    "expires_at": {
      "description": "Timezone-aware UTC timestamp serialized with a trailing Z.",
      "format": "date-time",
      "pattern": "(?:Z|[+-]\\d{2}:\\d{2})$",
      "title": "Expires At",
      "type": "string"
    },
    "generated_at": {
      "description": "Timezone-aware UTC timestamp serialized with a trailing Z.",
      "format": "date-time",
      "pattern": "(?:Z|[+-]\\d{2}:\\d{2})$",
      "title": "Generated At",
      "type": "string"
    },
    "intent": {
      "$ref": "#/$defs/SignalIntent"
    },
    "reason_codes": {
      "default": [],
      "items": {
        "maxLength": 64,
        "minLength": 2,
        "pattern": "^[A-Z][A-Z0-9_]{1,63}$",
        "type": "string"
      },
      "title": "Reason Codes",
      "type": "array"
    },
    "research_id": {
      "maxLength": 160,
      "minLength": 8,
      "pattern": "^[a-z][a-z0-9_]{1,12}_[A-Za-z0-9_-]{4,128}$",
      "title": "Research Id",
      "type": "string"
    },
    "schema_version": {
      "const": "1.0",
      "default": "1.0",
      "title": "Schema Version",
      "type": "string"
    },
    "signal_id": {
      "maxLength": 160,
      "minLength": 8,
      "pattern": "^[a-z][a-z0-9_]{1,12}_[A-Za-z0-9_-]{4,128}$",
      "title": "Signal Id",
      "type": "string"
    },
    "strategy": {
      "maxLength": 64,
      "minLength": 2,
      "pattern": "^[a-z][a-z0-9_]{1,63}$",
      "title": "Strategy",
      "type": "string"
    },
    "strategy_version": {
      "maxLength": 32,
      "minLength": 5,
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
      "title": "Strategy Version",
      "type": "string"
    },
    "strength": {
      "description": "Canonical decimal string without scientific notation. Binary JSON numbers are rejected.",
      "maxLength": 64,
      "pattern": "^-?(?:0|[1-9]\\d*)(?:\\.\\d+)?$",
      "title": "Strength",
      "type": "string"
    },
    "symbol": {
      "maxLength": 10,
      "minLength": 1,
      "pattern": "^[A-Z][A-Z0-9.]{0,9}$",
      "title": "Symbol",
      "type": "string"
    },
    "target_weight": {
      "anyOf": [
        {
          "description": "Canonical decimal string without scientific notation. Binary JSON numbers are rejected.",
          "maxLength": 64,
          "pattern": "^-?(?:0|[1-9]\\d*)(?:\\.\\d+)?$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Target Weight"
    }
  },
  "required": [
    "signal_id",
    "research_id",
    "strategy",
    "strategy_version",
    "symbol",
    "intent",
    "strength",
    "generated_at",
    "expires_at"
  ],
  "title": "TradeSignal",
  "type": "object"
}
