{
  "additionalProperties": false,
  "description": "Individual fill against a broker order.",
  "properties": {
    "broker_order_id": {
      "maxLength": 128,
      "minLength": 3,
      "title": "Broker Order Id",
      "type": "string"
    },
    "fill_id": {
      "maxLength": 128,
      "minLength": 3,
      "title": "Fill Id",
      "type": "string"
    },
    "filled_at": {
      "description": "Timezone-aware UTC timestamp serialized with a trailing Z.",
      "format": "date-time",
      "pattern": "(?:Z|[+-]\\d{2}:\\d{2})$",
      "title": "Filled At",
      "type": "string"
    },
    "price": {
      "description": "Canonical decimal string without scientific notation. Binary JSON numbers are rejected.",
      "maxLength": 64,
      "pattern": "^-?(?:0|[1-9]\\d*)(?:\\.\\d+)?$",
      "title": "Price",
      "type": "string"
    },
    "quantity": {
      "description": "Canonical decimal string without scientific notation. Binary JSON numbers are rejected.",
      "maxLength": 64,
      "pattern": "^-?(?:0|[1-9]\\d*)(?:\\.\\d+)?$",
      "title": "Quantity",
      "type": "string"
    },
    "schema_version": {
      "const": "1.0",
      "default": "1.0",
      "title": "Schema Version",
      "type": "string"
    }
  },
  "required": [
    "fill_id",
    "broker_order_id",
    "quantity",
    "price",
    "filled_at"
  ],
  "title": "BrokerFill",
  "type": "object"
}
